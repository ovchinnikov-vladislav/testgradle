subprojects {
    apply plugin: 'java'
    apply plugin: 'groovy'

    repositories {
        jcenter()
    }

    dependencies {
        implementation gradleApi()
        implementation localGroovy()
    }
}